# openapi-typescript (a dev-only tool) pins its peer to typescript@^5.x, but the
# project runs typescript@6. The mismatch is harmless here, so accept it instead
# of pinning the whole toolchain back to TS 5. Applies to local installs and the
# container build alike (npm reads .npmrc from the working dir).
legacy-peer-deps=true
