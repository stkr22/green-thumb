name: Helm Chart Release

# Publishes the green-thumb Helm chart as an OCI artifact to GHCR on every
# GitHub Release, alongside the container image (see container-build-push.yml).
# The chart lands at oci://ghcr.io/<owner>/charts/green-thumb:<version>.
on:
  release:
    types: [published]

jobs:
  release-chart:
    runs-on: ubuntu-latest

    permissions:
      packages: write
      contents: read

    env:
      CHART_DIR: charts/green-thumb
      # OCI repository the chart is pushed into (chart name is appended by helm).
      OCI_REPO: oci://ghcr.io/${{ github.repository_owner }}/charts

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6

      - name: Derive version from tag
        # Strip a leading "v": v0.1.0 -> 0.1.0. SemVer is required by OCI/Helm.
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      # helm is pre-installed on GitHub-hosted ubuntu runners (>= 3.8, OCI GA).
      - name: Lint chart
        run: helm lint "$CHART_DIR"

      - name: Package chart
        # Pin both chart version and appVersion to the release so the deployed
        # image tag (which defaults to .Chart.AppVersion) matches the chart.
        run: |
          helm package "$CHART_DIR" \
            --version "$VERSION" \
            --app-version "$VERSION" \
            --destination dist

      - name: Login to GitHub Container Registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" \
            | helm registry login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Push chart
        run: helm push "dist/green-thumb-${VERSION}.tgz" "$OCI_REPO"
